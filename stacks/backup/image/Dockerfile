# --- Imagen ---
# Sin capas propias: el stack no necesita nada que la imagen oficial no traiga ya.
# Los scripts llegan por bind-mount, no por COPY — son runtime, no parte de la imagen.

FROM restic/restic:0.19.1
