# --- Proveedor de dashboards ---
# Carga los JSON prearmados del directorio; quedan de solo lectura en la UI.

apiVersion: 1

providers:
  - name: prearmados
    type: file
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
